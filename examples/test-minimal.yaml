openapi: 3.0.0
info:
  title: Minimal API
  version: 1.0.0
  description: A minimal OpenAPI spec with just a basic GET endpoint

servers:
  - url: https://api.minimal.com

paths:
  /health:
    get:
      summary: Health Check
      responses:
        '200':
          description: OK
